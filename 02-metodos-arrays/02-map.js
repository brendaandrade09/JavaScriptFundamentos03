const numeros = [1, 2, 3, 4, 5];
const dobrado = numeros.map((num) => num * 2);
console.log(dobrado);

let nomes = ["Beren", "Mel", "Lukz", "Jack"];
let mensagem = nomes.map((nome) => `Olá, ${nome.toUpperCase()}!`);
console.log(mensagem);